/**
 *
 * ListContainer
 *
 */
import React, { memo } from 'react';
import styled from 'styled-components/macro';

interface Props {}

export const ListContainer = memo((props: Props) => {
  return <Div></Div>;
});

const Div = styled.div``;
